server:
  port: 8080

spring:
  application:
    name: word-online-server

kakao:
  client-id: c77f24055af882d026f5a4135d62f6cf
  redirect-uri: http://localhost:8080/api/auth/kakao/callback

logging:
  level:
    org:
      springframework:
        web:
          socket:
            INFO